# Task: profile-media metadata sync

- ID / project: Exotic CRM + local `exotic-crm-sync` WordPress plugin
- Updated / status: 2026-09-22 / CRM pushed; WordPress plugin committed locally; neither deployed
- Goal and acceptance criteria: New image and video uploads receive the profile title and a Media Library description assembled from location, calculated adult age, and bio. Saving those source profile fields refreshes existing attachment metadata. CRM Media tab shows the applied metadata.
- Scope authorized by the current request: CRM Media UI/build artifact and the local WordPress sync plugin only; no production changes.
- Source plan / decisions / relevant code: `resources/js/pages/ClientDetail.jsx`; WP plugin `includes/class-media-endpoint.php` and `includes/class-client-update-endpoint.php`. Attachment description means the WordPress `post_content` field, not the profile page HTML meta-description tag.
- History reconciliation: CRM `85e3c696` adds the Media-tab UI/build; WordPress plugin `f57f18b76` adds attachment synchronization. Neither predecessor contained title/description synchronization. Existing plugin already receives CRM-generated image alt text on upload.
- Done with evidence: New uploads call the metadata synchronizer; profile saves limited to name/title, bio/content, birthday, or location fields resync existing attachments; image and video coverage added in `tests/MediaEndpointTest.php`; plugin list response exposes `title` and `description`; CRM frontend build embeds the updated UI copy.
- In progress / remaining: WordPress unit suite could not start because its configured `wordpress-tests-lib` bootstrap directory is absent. Validate on a local WP test install before plugin packaging and deployment.
- Next concrete action or command: Restore/configure `WP_PHPUNIT__DIR` to a WordPress test library, then run `/usr/local/opt/php@8.2/bin/php vendor/bin/phpunit --filter MediaEndpointTest` from the plugin root.
- Verification: 2026-09-22 at CRM HEAD `bbe2e6c0`: PHP 8.2 lint passed for changed plugin files/test; `git diff --check` passed in both repositories; `npm run build` passed (retained pre-existing CSS parser/chunk-size warnings); matching Vite asset contains the new UI copy. Plugin PHPUnit attempted but failed before test discovery: missing `/var/folders/.../wordpress-tests-lib/includes/functions.php`.
- Deployment: CRM `85e3c696` pushed to `origin/main` at 2026-09-22; await cPanel pull, then browser verification. Plugin `f57f18b76` and changelog `aabd6f0d3` are committed locally in the WordPress repository; no plugin package, WordPress push, cPanel upload, or live verification.
- Files/hunks owned; unrelated working-tree/index changes to preserve: CRM `resources/js/pages/ClientDetail.jsx`, generated `public/build/manifest.json` and new assets; local plugin `includes/class-media-endpoint.php`, `includes/class-client-update-endpoint.php`, `tests/MediaEndpointTest.php`. Preserve the pre-existing CRM deleted runbook and untracked plans/output artifacts.
- Unresolved questions / blockers: Attachment metadata is not equivalent to a profile page meta tag and is a secondary SEO signal; attachment pages should remain noindex/redirected if they are thin.
